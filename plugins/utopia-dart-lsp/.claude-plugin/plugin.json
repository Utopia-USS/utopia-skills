{
  "name": "utopia-dart-lsp",
  "description": "👾 Dart/Flutter language server (LSP) for Claude Code. Live analyzer diagnostics injected on every .dart edit plus hover, goto-definition, find-references, workspace symbols and call hierarchy. fvm-aware: prefers a project-pinned .fvm SDK, falls back to fvm, then the dart on PATH.",
  "version": "0.1.1",
  "author": {
    "name": "UtopiaSoftware"
  },
  "license": "BSD-2-Clause",
  "lspServers": {
    "dart": {
      "command": "sh",
      "args": ["-c", "exec \"${CLAUDE_PLUGIN_ROOT}/scripts/dart_lsp.sh\""],
      "extensionToLanguage": {
        ".dart": "dart"
      },
      "initializationOptions": {
        "onlyAnalyzeProjectsWithOpenFiles": true,
        "suggestFromUnimportedLibraries": false,
        "closingLabels": true,
        "flutterOutline": true,
        "outline": true
      }
    }
  }
}
